{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adl-spec.org/profiles/healthcare/1.0/schema.json",
  "title": "ADL Healthcare Profile",
  "description": "JSON Schema for ADL documents declaring the healthcare profile (urn:adl:profile:healthcare:1.0). Extends the base ADL schema via allOf composition. Designed to compose with the governance profile, so the top level is not sealed.",
  "$defs": {
    "extensions": {
      "type": "object",
      "patternProperties": {
        "^[a-z][a-z0-9-]*(\\.[a-z][a-z0-9-]*)+$": {
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": false,
      "description": "Vendor-namespaced extensions. Keys are reverse-domain identifiers."
    }
  },
  "allOf": [
    { "$ref": "https://adl-spec.org/0.3/schema.json" }
  ],
  "type": "object",
  "required": ["hipaa_compliance", "phi_handling", "data_classification"],
  "properties": {
    "profiles": {
      "type": "array",
      "contains": { "const": "urn:adl:profile:healthcare:1.0" }
    },
    "hipaa_compliance": {
      "type": "object",
      "description": "HIPAA regulatory compliance configuration. REQUIRED when using this profile.",
      "required": ["covered_entity_type", "baa_required", "minimum_necessary"],
      "properties": {
        "covered_entity_type": {
          "type": "string",
          "enum": ["covered_entity", "business_associate", "subcontractor"]
        },
        "baa_required": { "type": "boolean" },
        "minimum_necessary": {
          "type": "object",
          "required": ["scope"],
          "properties": {
            "scope": { "type": "string", "enum": ["task_specific", "role_based", "full_record"] },
            "justification": { "type": "string" },
            "review_frequency": { "type": "string" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "security_rule": {
          "type": "object",
          "properties": {
            "encryption_at_rest": { "type": "string", "enum": ["AES_256", "AES_128"] },
            "encryption_in_transit": { "type": "string", "enum": ["TLS_1_2", "TLS_1_3"] },
            "mfa_required": { "type": "boolean" },
            "data_retention": { "type": "string", "enum": ["none", "minimum", "standard"] },
            "restoration_hours": { "type": "number", "minimum": 0 },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "extensions": { "$ref": "#/$defs/extensions" }
      },
      "additionalProperties": false
    },
    "phi_handling": {
      "type": "object",
      "description": "PHI data handling and de-identification controls. REQUIRED when using this profile.",
      "required": ["de_identification", "breach_notification"],
      "properties": {
        "de_identification": {
          "type": "object",
          "required": ["method"],
          "properties": {
            "method": { "type": "string", "enum": ["safe_harbor", "expert_determination", "none"] },
            "re_identification_controls": { "type": "boolean" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "breach_notification": {
          "type": "object",
          "required": ["notification_hours", "contact"],
          "properties": {
            "notification_hours": { "type": "number", "minimum": 0 },
            "contact": { "type": "string", "minLength": 1 },
            "threshold": { "type": "number", "minimum": 0 },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "consent_management": {
          "type": "object",
          "properties": {
            "required": { "type": "boolean" },
            "consent_types": {
              "type": "array",
              "items": {
                "type": "string",
                "enum": ["treatment", "payment", "operations", "research", "marketing"]
              }
            },
            "granularity": { "type": "string", "enum": ["broad", "purpose_specific", "data_specific"] },
            "revocation_supported": { "type": "boolean" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "data_provenance": {
          "type": "object",
          "properties": {
            "tracking": { "type": "boolean" },
            "source_systems": { "type": "array", "items": { "type": "string" } },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "extensions": { "$ref": "#/$defs/extensions" }
      },
      "additionalProperties": false
    },
    "clinical_safety": {
      "type": "object",
      "description": "Clinical safety controls for decision support or patient-facing agents.",
      "properties": {
        "fda_classification": {
          "type": "object",
          "properties": {
            "device_class": {
              "type": "string",
              "enum": ["exempt", "class_I", "class_II", "class_III", "non_device"]
            },
            "clearance_type": {
              "type": "string",
              "enum": ["510k", "de_novo", "pma", "not_applicable"]
            },
            "clearance_number": { "type": "string" },
            "software_level": {
              "type": "string",
              "enum": ["non_significant_risk", "significant_risk", "life_supporting"]
            },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "change_control": {
          "type": "object",
          "properties": {
            "pccp_authorized": { "type": "boolean" },
            "modification_scope": { "type": "array", "items": { "type": "string" } },
            "validation_protocol": { "type": "string", "format": "uri" },
            "rollback_plan": { "type": "boolean" },
            "monitoring_frequency": { "type": "string" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "bias_monitoring": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "protected_classes": { "type": "array", "items": { "type": "string" } },
            "assessment_frequency": { "type": "string" },
            "last_assessment": { "type": "string", "format": "date-time" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "human_in_the_loop": {
          "type": "object",
          "properties": {
            "level": {
              "type": "string",
              "enum": ["advisory", "approval_required", "continuous_oversight"]
            },
            "role": { "type": "string" },
            "escalation_path": { "type": "string" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "extensions": { "$ref": "#/$defs/extensions" }
      },
      "additionalProperties": false
    },
    "interoperability": {
      "type": "object",
      "description": "Health data interoperability standards compliance.",
      "properties": {
        "fhir_version": {
          "type": "string",
          "enum": ["DSTU2", "STU3", "R4", "R4B", "R5"]
        },
        "terminology_bindings": { "type": "array", "items": { "type": "string" } },
        "tefca_participant": { "type": "boolean" },
        "information_blocking": {
          "type": "object",
          "properties": {
            "compliant": { "type": "boolean" },
            "exceptions_claimed": { "type": "array", "items": { "type": "string" } },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "dsi_transparency": {
          "type": "object",
          "properties": {
            "predictive_dsi": { "type": "boolean" },
            "source_attributes_published": { "type": "boolean" },
            "irm_practices": { "type": "boolean" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        },
        "extensions": { "$ref": "#/$defs/extensions" }
      },
      "additionalProperties": false
    },
    "data_classification": {
      "type": "object",
      "description": "Core data_classification, tightened: MUST carry a healthcare sub-object and include the phi category.",
      "required": ["categories", "healthcare"],
      "properties": {
        "categories": {
          "type": "array",
          "contains": { "const": "phi" }
        },
        "healthcare": {
          "type": "object",
          "required": ["phi_types"],
          "properties": {
            "phi_types": {
              "type": "array",
              "minItems": 1,
              "items": {
                "type": "string",
                "enum": [
                  "demographics", "medical_records", "billing", "mental_health",
                  "substance_use", "genetic", "reproductive", "hiv_status"
                ]
              }
            },
            "hipaa_applicability": { "type": "boolean" },
            "extensions": { "$ref": "#/$defs/extensions" }
          },
          "additionalProperties": false
        }
      }
    }
  }
}
