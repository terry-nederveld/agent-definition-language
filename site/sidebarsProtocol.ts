import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebarsProtocol: SidebarsConfig = {
  protocolSidebar: [
    {type: 'doc', id: 'index', label: 'Overview'},
    {type: 'doc', id: 'trust-protocol', label: 'Trust Protocol'},
    {type: 'doc', id: 'runtime-protocol', label: 'Runtime Protocol'},
  ],
};

export default sidebarsProtocol;
